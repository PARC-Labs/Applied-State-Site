---
interface Props {
  size?: 'small' | 'medium' | 'large'
}

const { size = 'medium' } = Astro.props
---

<div class:list={['editorial-spacer', `editorial-spacer--${size}`]} aria-hidden="true"></div>
